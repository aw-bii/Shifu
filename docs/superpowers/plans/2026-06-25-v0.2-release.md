# v0.2 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finalize and ship the MyRA v0.2 release — commit artefacts, write the changelog, bump the version, verify the build, package the installer, and tag the release.

**Architecture:** All implementation work from the six v0.2 improvement plans is already in the codebase. This plan only handles release finalization: committing plan documents, writing the user-facing CHANGELOG, bumping `package.json`, verifying the build and packaging pipeline, and creating the annotated git tag.

**Tech Stack:** Git, Node.js / npm, electron-builder, TypeScript

## Global Constraints

- `npm test` must pass (238 tests) before tagging
- `npm run build` must complete with no TypeScript errors before tagging
- Version string `0.2.0` must appear only in `package.json` and `CHANGELOG.md` — no source file hardcoding
- Tag must be annotated (`git tag -a`), not lightweight

---

## File Map

| File | Change |
|------|--------|
| `docs/superpowers/plans/*.md` (6 files) | Commit — currently untracked |
| `CHANGELOG.md` | Create at repo root |
| `package.json` | Bump `version` field: `0.1.0 → 0.2.0` |

---

## Task 1: Commit Plan Artefacts

**Files:**
- Commit (untracked): `docs/superpowers/plans/2026-06-25-adapt-responsive.md`
- Commit (untracked): `docs/superpowers/plans/2026-06-25-app-flow-audit-fixes.md`
- Commit (untracked): `docs/superpowers/plans/2026-06-25-audit-fixes.md`
- Commit (untracked): `docs/superpowers/plans/2026-06-25-colorize-design-tokens.md`
- Commit (untracked): `docs/superpowers/plans/2026-06-25-harden-accessibility.md`
- Commit (untracked): `docs/superpowers/plans/2026-06-25-optimize-performance.md`

**Interfaces:**
- Consumes: nothing
- Produces: all six plan docs tracked in git history

- [ ] **Step 1: Verify the six plan files are present and untracked**

```bash
git status docs/superpowers/plans/
```

Expected: six `??` (untracked) entries.

- [ ] **Step 2: Stage and commit the plan docs**

```bash
git add docs/superpowers/plans/
git commit -m "docs: commit v0.2 implementation plan artefacts"
```

Expected: commit succeeds, `git status` shows clean working tree for that directory.

---

## Task 2: Write CHANGELOG and Bump Version

**Files:**
- Create: `CHANGELOG.md`
- Modify: `package.json` (line 3: `"version"` field)

**Interfaces:**
- Consumes: nothing
- Produces: `CHANGELOG.md` at repo root; `package.json` version = `"0.2.0"`

- [ ] **Step 1: Create CHANGELOG.md**

Create `CHANGELOG.md` at the repo root with the following content (copy verbatim):

```markdown
# Changelog

## [0.2.0] — 2026-06-25

### Bug Fixes
- Panels (Personas, Pipelines, Settings) now properly collapse to zero width — chat area fills available space when panels are closed
- Close button added to Personas and Pipelines panels
- Wizard sign-in instructions are hidden when all tools are already authenticated
- Backend switcher warns you when the selected backend needs sign-in before use
- Security dialogs correctly re-focus on the first button when multiple alerts arrive in sequence
- Settings panel loads gracefully even if individual IPC calls fail on startup
- Pipeline security events reach the renderer even after a window reload mid-run
- Conversation search no longer fires a stale setState after the component unmounts
- Wizard install output lines carry the backend id so the renderer can route them correctly

### Performance
- Search results update after you pause typing (300 ms debounce) — not on every keystroke
- Settings panel fetches all data in one parallel round-trip instead of four sequential ones
- Message bubbles skip re-renders when only unrelated fields change; attachment cache is bounded at 100 entries
- Message list skips re-renders on unrelated parent updates
- Backend availability and auth checks run in parallel per adapter

### Accessibility
- All toolbar buttons have accessible labels (`aria-label`) and toggle state (`aria-pressed`)
- Press F2 on a conversation to enter rename mode from the keyboard
- Only one of Send / Stop exists in the DOM at a time — screen readers no longer see a hidden duplicate button
- Focus is trapped inside security alert dialogs and returns to the first button when a new alert arrives
- Pipeline step tabs use `role="tablist"` / `role="tab"` / `aria-selected` / `aria-controls`
- A skip-to-main-content link is visible on keyboard focus at the top of the app
- Streaming assistant responses are announced to screen readers via a `role="status"` live region
- Message bubbles carry `role="article"` with an accessible label; timestamps use a `<time>` element
- Interactive persona items are native `<button>` elements instead of `div role="button"`

### Design System
- All component colors now use CSS custom property tokens (`--c-primary`, `--c-bubble`, etc.)
- Tailwind semantic utility classes registered: `bg-primary`, `bg-bubble`, `text-text-muted`, `border-border`, and more
- Dark mode handled by CSS variable flipping in `.dark` — no more paired `dark:` Tailwind variants

### Responsive
- Sidebar panels clamp to `min(Npx, 80vw)` — never overflow on narrow viewports
- Toolbar wraps to a second row on small screens instead of scrolling horizontally
- Long-press (600 ms) on a conversation triggers rename on touch devices
- Input textarea height caps at `min(10rem, 40vh)` — shrinks proportionally on mobile
- Touch targets meet the 44 px minimum on coarse-pointer devices

## [0.1.0] — 2026-06-17

Initial release.
```

- [ ] **Step 2: Bump the version in package.json**

Open `package.json`. On line 3, change:

```json
"version": "0.1.0",
```

to:

```json
"version": "0.2.0",
```

No other lines change.

- [ ] **Step 3: Verify only the expected files are modified**

```bash
git diff --name-only
```

Expected output:
```
CHANGELOG.md
package.json
```

If any other files appear, investigate before proceeding.

- [ ] **Step 4: Run the full test suite**

```bash
npm test
```

Expected: `238 passed`, zero failures.

- [ ] **Step 5: Commit**

```bash
git add CHANGELOG.md package.json
git commit -m "chore: bump version to 0.2.0 and add CHANGELOG"
```

---

## Task 3: Verify Build

**Files:**
- No file changes — verification only

**Interfaces:**
- Consumes: `package.json` at `0.2.0`
- Produces: confirmed clean TypeScript compilation

- [ ] **Step 1: Run the TypeScript build**

```bash
npm run build
```

Expected: exits 0, no TypeScript errors in output. The `out/` directory is populated with compiled main and renderer bundles.

If the build fails: do not proceed to Task 4. Read the error, fix the root cause, and re-run before continuing.

---

## Task 4: Package the Installer

**Files:**
- No source file changes — packaging only

**Interfaces:**
- Consumes: compiled output in `out/` from Task 3
- Produces: Windows installer in `dist/`

- [ ] **Step 1: Run electron-builder**

```bash
npm run dist
```

This runs `npm run build && electron-builder`. Expected: exits 0. Inspect `dist/` for an `.exe` installer (NSIS) or `.msi` file.

```bash
ls dist/
```

Expected: at least one installer file present (e.g. `MyRA Setup 0.2.0.exe`).

If packaging fails: read the electron-builder error. Common causes are missing icon assets or a code-signing misconfiguration — do not tag until packaging succeeds.

---

## Task 5: Create the Release Tag

**Files:**
- No file changes — git tag only

**Interfaces:**
- Consumes: the version-bump commit from Task 2
- Produces: annotated tag `v0.2.0` on that commit

- [ ] **Step 1: Confirm HEAD is the version-bump commit**

```bash
git log --oneline -3
```

Expected: the top commit message is `chore: bump version to 0.2.0 and add CHANGELOG`.

- [ ] **Step 2: Create the annotated tag**

```bash
git tag -a v0.2.0 -m "MyRA v0.2.0"
```

Expected: no output (success).

- [ ] **Step 3: Verify the tag**

```bash
git tag -l "v0.2.0" -n1
```

Expected:
```
v0.2.0          MyRA v0.2.0
```

---

## Self-Review

**Spec coverage:**

| Spec requirement | Task |
|---|---|
| Commit 6 plan documents | Task 1 ✅ |
| Write user-facing CHANGELOG | Task 2 ✅ |
| Bump version `0.1.0 → 0.2.0` in `package.json` | Task 2 ✅ |
| `npm test` passes before tagging | Task 2 Step 4 ✅ |
| `npm run build` succeeds | Task 3 ✅ |
| `npm run dist` produces installer | Task 4 ✅ |
| Annotated git tag `v0.2.0` | Task 5 ✅ |

**Placeholder scan:** No TBD, TODO, or "similar to" language. All steps contain exact commands or exact file content.

**Type consistency:** N/A — no new types introduced.
